import { Section } from "@/components/layout/Section";
import { Container } from "@/components/layout/Container";

/**
 * HowItWorks section — implement pixel-perfect from Figma designs.
 * All values (colors, spacing, radius) must use CSS token variables.
 */
export function HowItWorks() {
  return (
    <Section>
      <Container>
        {/* TODO: implement from Figma */}
      </Container>
    </Section>
  );
}
